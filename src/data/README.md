# `src/data/`

Portfolio content here is generated from `~/projects/my-portfolio`'s `lib/data/`, which is the source of truth. Keep this directory honest by running `bun run scripts/sync-content.ts` (or `bun run sync-content`); hand-edits will be overwritten on the next sync.
